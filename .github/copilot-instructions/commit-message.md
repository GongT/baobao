# 规则

* 每个包一行，每行不应过长
* 每个包出现最多一次
* “包”指的是 monorepo 中的各个子包，特点是存放在以@开头的目录中。
* 包中的每个文件，如果修改少于或等于2处，且每处修改不多于10行，则认为该文件未修改。
* 如果提交中包含非包文件，则在提交信息最后添加它们

## 示例提交

```
[@idlebox/node] 修复 README 中的一个拼写错误
[@build-script/monorepo-lib] 添加对 package.yaml 的支持

更新 README，添加新的使用示例
修复了一个workflow中的bug
```
