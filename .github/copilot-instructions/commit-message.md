每个包一行，每行不应过长，格式如下:

```
[@idlebox/node] 修复 README 中的一个拼写错误
[@build-script/monorepo-lib] 添加对 package.yaml 的支持
```

“包”指的是 monorepo 中的各个子包，特点是存放在以@开头的目录中。

任何单一文件中：如果修改少于或等于2个，并且任何一个都不多于10行，则认为该文件未修改。

如果提交中包含非包文件，则写在上述信息之后，并在前面空一行，例如:

```
[@pkg/a] 修复某某功能
[@pkg/b] 修复某某功能

更新 README，添加新的使用示例
```
