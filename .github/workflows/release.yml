name: 发布新版本
run-name: ${{ github.actor }} 提交了一个新的节点版本 🚀

permissions:
  id-token: write
  contents: read

on:
  push:
    branches: ['master']
  workflow_dispatch:
    inputs:
      skip_check:
        description: 跳过“检测修改并提交”步骤
        required: false
        type: boolean
        default: false

jobs:
  cache:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, '[checkpoint] ') || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: 安装pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
          version: 10.28.0 # pnpm版本号

      - name: 安装Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 25 # node版本号
          check-latest: true
          cache: 'pnpm'

      - name: 安装依赖
        run: |
          pnpm --global install npm@latest
          pnpm install --frozen-lockfile
          pnpm store prune --force

  build:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, '[checkpoint] ') || github.event_name == 'workflow_dispatch'
    needs: cache
    permissions:
      contents: write
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 4

      - name: 安装pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false
          version: 10.28.0 # pnpm版本号

      - name: 安装Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 25 # node版本号
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: 安装依赖
        run: |
          git clean -ffdx
          pnpm --global install npm@latest
          which npm
          npm --version
          pnpm install --frozen-lockfile
          pnpm store prune --force

      - name: 执行构建
        run: |
          npm config --location=user set cache="${RUNNER_TEMP}/npm"
          pnpm run build

      - name: 检测修改并提交
        # if: "!inputs.skip_check"
        run: source .github/scripts/check-commit.sh

      # - name: 跳过检测打包
      #   if: "inputs.skip_check"
      #   run: pnpm run ???

      - name: 发布新版本
        run: source .github/scripts/publish-all-tarball.sh
      