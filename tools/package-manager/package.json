{
	"monorepo": true,
	"name": "@idlebox/what-pm",
	"version": "1.0.0",
	"scripts": {
		"build": "tsc -p src",
		"prepare": "im-single-dog",
		"prepublish": "rimraf lib dist && npm run build",
		"upgrade": "ncu --upgrade --packageFile ./package.json",
		"watch": "tsc -w -p src"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/node": "^12.0.12",
		"@gongt/single-dog": "^2.0.2",
		"npm-check-updates": "^3.1.13",
		"typescript": "^3.5.2",
		"rimraf": "^2.6.3"
	}
}