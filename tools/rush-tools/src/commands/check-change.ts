import { description } from '../common/description';

export default async function runCheckChange() {

}

description(runCheckChange, 'Build every project, compare with their published files. If changed, increase their version in package.json');
