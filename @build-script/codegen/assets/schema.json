{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "Codegen plugin for heft",
	"description": "This schema describes the \"options\" field that can be specified in heft.json when loading \"@build-script/codegen\".",
	"type": "object",

	"additionalProperties": false,
	"required": ["project"],
	"properties": {
		"project": {
			"title": "Project Path",
			"description": "path to tsconfig.json",
			"type": "string"
		}
	}
}
