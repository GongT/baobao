import { description } from '../../common/description';

description(createVSCode, 'Create/Sync VSCode workspace file');

export default async function createVSCode() {
	throw new Error('Not Impl.');
}
