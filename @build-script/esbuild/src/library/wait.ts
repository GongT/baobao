// const selfNotify = resolve(distDir, 'compile.lock');

export async function waitComplete() {
	// let i = 0;
	// while (true) {
	// 	if (await pathExists(selfNotify)) {
	// 		await stat(selfNotify);
	// 	}
	// 	process.stderr.write(`\x1B[38;5;11mwaitting compile... (${i++})\x1B[0m\r`);
	// 	await sleep(1000);
	// }
}
export async function emitComplete() {}
