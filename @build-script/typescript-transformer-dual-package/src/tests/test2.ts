import ts, { createProgram as X } from 'typescript';
export { createProgram as Y } from 'typescript';
import { createCompilerHost } from 'typescript';
import { createInputFiles as rename } from 'typescript';
import * as __TS__ from 'typescript';

if (1 + 1 > 2) {
	console.log(X);
	console.log(ts);
	console.log(createCompilerHost);
	console.log(rename);
	console.log(__TS__);
}
