{
	"name": "@build-script/rollup-plugin-module-import-dew",
	"keywords": [],
	"type": "commonjs",
	"description": "Convert (TypeScript compiled) module's import statements, to work with jspm dew file.",
	"version": "1.0.8",
	"main": "dist/api.js",
	"types": "dist/api.d.ts",
	"license": "MIT",
	"scripts": {
		"prepack": "rm -rf dist && tsc -p src",
		"watch": "tsc -p src -w",
		"build": "tsc -p src",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@jspm/resolve": "^1.0.0",
		"source-map": "^0.7.3"
	},
	"devDependencies": {
		"@types/node": "^14.10.2",
		"typescript": "^4.0.2"
	},
	"repository": "https://github.com/GongT/baobao"
}
