{
	"$schema": "../../../@mpis/run/commands.schema.json",
	"build": ["typescript", "esbuild"],
	"commands": {
		"esbuild": {
			"title": "esbuild",
			"command": {
				"package": "@mpis/esbuild",
				"binary": "mpis-esbuild",
				"arguments": ["scripts/mpis-build.js"],
			},
		},
	},
}
