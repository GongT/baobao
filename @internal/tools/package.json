{
	"name": "@internal/plugins",
	"type": "commonjs",
	"version": "0.0.0",
	"license": "WTFPL",
	"private": true,
	"scripts": {
		"build": "local-esbuild",
		"build:watch": "local-esbuild --fast",
		"typecheck": "tsc -p src -noEmit",
		"clean": "rimraf temp lib",
		"watch": "local-esbuild --watch"
	},
	"devDependencies": {
		"@build-script/single-dog-asset": "workspace:^",
		"@types/node": "^22.7.4",
		"rimraf": "^6.0.1",
		"typescript": "^5.6.3"
	},
	"dependencies": {
		"execa": "^9.4.0",
		"glob": "^11.0.0"
	}
}
